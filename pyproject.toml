[project]
name = "priority-fairness-concurrency-demo"
version = "0.1.0"
description = "Interactive Temporal task queue priority, Fairness, and concurrency demo"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.56.0",
    "temporalio>=1.26.0",
]
