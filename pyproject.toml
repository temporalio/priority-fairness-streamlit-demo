[project]
name = "priority-fairness-replay"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.56.0",
    "temporalio>=1.26.0",
]
